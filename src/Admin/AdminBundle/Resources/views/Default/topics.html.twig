{% extends "AdminAdminBundle::layout.html.twig" %}

{% block content %}
           <!-- Page Content -->
        <div class="tab-content">
                <div id="page-wrapper">
                    <div class="container-fluid">
                        <div class="row">
                            <div class="col-lg-12">
                                <h1 class="page-header">Sujets</h1>
                                <div class="col-lg-10">
                                <div class="form-group">
                            <label>Ajouter un sujet</label>
                            {{ include("AdminAdminBundle:Default:formTopic.html.twig") }}
                            <ul>                 
                                <div class="col-md-11 postopic">  
                                {% for names in listTopic %}
                                <div class="panel panel-default">
                            <div class="panel-heading">
                            </div>
                            <div class="panel-body">

                                <div class="col-md-12 cat">                                    
                                    <li><div class="box1">Catégorie: {{ names.tag }}</div><div lass="box2">Titre: {{ names.Name }}</div><div class="col-md-1 del">
                        <a href="{{ path('admin_admin_delete', {'id': names.id}) }}" class="btn btn-outline btn-primary">Effacer</a>
                    </div></li>
                                </div>
                            </div>
                        </div> 
                                {% endfor %}
                                </div>
                            </ul>
                        </div>
                                </div>
                            </div>
                            <!-- /.col-lg-12 -->
                        </div>
                        <!-- /.row -->
                    </div>
                    <!-- /.container-fluid -->
                </div>
            </div>

{% endblock %}