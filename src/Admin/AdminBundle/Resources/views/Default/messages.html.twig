{% extends "AdminAdminBundle::layout.html.twig" %}

{% block content %}
           <!-- Page Content -->
        <div class="tab-content">
                <div id="page-wrapper">
                    <div class="container-fluid">
                        <div class="row">
                            <div class="col-lg-12">
                                <h1 class="page-header">Messages</h1>
                                <div class="col-lg-10">
                                <div class="form-group">
                            <ul>                 
                                <div class="col-md-11 postopic">  
                                {% for post in postPages %}
                                <div class="panel panel-default">
                            <div class="panel-heading">
                            </div>
                            <div class="panel-body">

                                <div class="col-md-12 cat">                                    
                                    <li><div class="box1">Sujet: {{ post.topics.name }}</div><div lass="box2">Message: {{ post.message }}</div>
                                        <div class="box3">Auteur: {{ post.usernames.username }}</div>
                                        <div class="col-md-1 del">
                        <a href="{{ path('admin_admin_delete', {'id': post.id, 'page': 'Post'}) }}" class="btn btn-outline btn-primary">Effacer</a>
                    </div></li>
                                </div>
                            </div>
                        </div> 
                                {% endfor %}
                                <div class="text-center">{{ knp_pagination_render(postPages) }}</div>
                                </div>
                            </ul>
                        </div>
                                </div>
                            </div>
                            <!-- /.col-lg-12 -->
                        </div>
                        <!-- /.row -->
                    </div>
                    <!-- /.container-fluid -->
                </div>
            </div>

{% endblock %}